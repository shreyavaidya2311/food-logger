import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CameraScreen = () => {
  return (
    <View>
      <Text>I am the camera</Text>
    </View>
  );
};

export default CameraScreen;

const styles = StyleSheet.create({});
